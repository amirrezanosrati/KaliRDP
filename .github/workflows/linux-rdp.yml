- name: Lightweight Ubuntu Desktop
  run: |
    # نصب نسخه سبک‌تر
    sudo apt-get install -y --no-install-recommends \
      xubuntu-desktop \
      xrdp \
      firefox
    
    echo "xfce4-session" > ~/.xsession
    sudo service xrdp restart
